PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Gmail used to SEND the notification email (needs a Gmail App Password, not your normal password)
SMTP_USER=youraddress@gmail.com
SMTP_PASS=your16characterapppassword

# Where contact-form messages should be delivered (defaults to SMTP_USER if left blank)
CONTACT_RECEIVER_EMAIL=saxenadivyanka187@gmail.com
